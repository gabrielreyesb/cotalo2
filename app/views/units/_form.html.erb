<%= form_with(model: unit, class: "unit-form") do |form| %>
  <% if unit.errors.any? %>
    <div class="alert alert-danger">
      <h4><%= pluralize(unit.errors.count, "error") %> impidieron que esta unidad se guardara:</h4>
      <ul>
        <% unit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, "Nombre", class: "form-label" %>
    <%= form.text_field :name, class: "form-control", required: true %>
    <small class="form-text text-muted">Ejemplo: Metro, Kilogramo, etc.</small>
  </div>
  
  <div class="mb-3">
    <%= form.label :abbreviation, "Abreviación", class: "form-label" %>
    <%= form.text_field :abbreviation, class: "form-control", required: true %>
    <small class="form-text text-muted">Ejemplo: m, kg, etc.</small>
  </div>
  
  <div class="d-flex justify-content-end">
    <%= form.submit "Guardar", class: "btn btn-primary" %>
  </div>
<% end %> 