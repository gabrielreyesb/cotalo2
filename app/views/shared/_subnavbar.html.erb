<nav class="navbar navbar-expand-lg navbar-dark bg-dark subnavbar">
  <div class="container-fluid py-0">
    <h1 class="navbar-brand mb-0">
      <% if content_for?(:page_title) %>
        <%= yield :page_title %>
      <% elsif controller_name == 'home' && action_name == 'index' %>
        Página principal
      <% else %>
        <%= controller_name.humanize.titleize %>
      <% end %>
    </h1>
    <div class="ms-auto">
      <%= yield :page_actions %>
    </div>
  </div>
</nav> 