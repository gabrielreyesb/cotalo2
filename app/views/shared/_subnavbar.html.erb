<nav class="navbar navbar-expand-lg navbar-dark bg-dark subnavbar">
  <div class="container-fluid py-0">
    <h1 class="navbar-brand mb-0">
      <% if content_for?(:page_title) %>
        <%= yield :page_title %>
      <% elsif controller_name == 'home' && action_name == 'index' %>
        Página principal
      <% else %>
        <%= controller_name.humanize.titleize %>
      <% end %>
    </h1>
    <div class="ms-auto">
      <%= yield :page_actions %>
    </div>
  </div>
</nav>

<style>
  .subnavbar {
    background-color: var(--cotalo-dark) !important;
    border-top: 1px solid rgba(255, 255, 255, 0.85);
    border-bottom: 1px solid rgba(255, 255, 255, 0.85);
    padding: 0.3rem 1rem;
    margin-bottom: 0.75rem;
    min-height: unset;
    position: relative;
    z-index: 2;  /* Below navbar but above main content */
  }
  
  .subnavbar .navbar-brand {
    font-size: 1.5rem;
    color: var(--cotalo-green);
    margin: 0;
    padding: 0;
    line-height: 1.2;
  }

  .subnavbar .btn {
    padding: 0.25rem 0.75rem;
    font-size: 0.9375rem;
    line-height: 1.2;
    margin: 0;
    color: white;
    border-color: rgba(255, 255, 255, 0.85);
  }

  .subnavbar .btn:hover {
    background-color: rgba(255, 255, 255, 0.1);
    color: white;
    border-color: white;
  }
</style> 